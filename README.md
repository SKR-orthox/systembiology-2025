
## 使い方（入力ファイルの指定）

このスクリプトは、指定したテキストファイル（タブ区切り）を読み込みます。

1. 解析に使いたいテキストファイルと、このスクリプトを **同じフォルダ** に置いてください。  
2. スクリプト冒頭の設定で、`INPUT_NAME` を入力ファイル名に合わせて変更します。

例：

INPUT_NAME = "input.txt"

を、実際のファイル名に変更します。

INPUT_NAME = "ChIP_KIT_vs_PS.txt"

この設定を変更した後にスクリプトを実行してください。
